//make runner && ./bin/rename -d -j ./benchmarks/gc.lang  => killed
//make runner && ./bin/rename -d -j -g ./benchmarks/gc.lang  => ok (estimated time 24s)

int some_func() {
    int n = 10; // push only with 10, check with 100000 +-
    for (int i = 0; i < n; i = i + 1) {
        int[n] array;
    }
}

int main() {
    some_func();
}
